[project]
name = "uni-speedrun"
version = "0.1.0"
description = "TUI-Dashboard zur Planung und Prognose eines Studienverlaufs"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

